line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]  # allow long lines if needed (formatter handles most)

[format]
quote-style = "double"
indent-style = "space"
